# Icons Directory

Place custom SVG icons here if needed.

The app currently uses Material Icons which are built-in.

## Custom Icons (Optional):
If you want to add custom icons:
1. Export as SVG from Figma/Illustrator
2. Use https://fluttershapemaker.com/ to convert to Flutter code
3. Or use flutter_svg package to load SVG files
